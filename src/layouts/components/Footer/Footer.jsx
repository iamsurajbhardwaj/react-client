import React from 'react';

const styles = {
  textAlign: 'center',
  marginTop: '35px',
};

const Footer = () => (
  <footer style={styles}>
    © Successive Technologies
  </footer>
);

export default Footer;
